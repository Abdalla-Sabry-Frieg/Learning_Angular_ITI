import { createAction } from "@ngrx/store";

export const increaseCounter = createAction("increase"); // increase name of the action

export const decreaseCounter = createAction("decrease"); // increase name of the action
